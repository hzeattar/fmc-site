{
	# FrankenPHP configuration for FMC site
	admin off
	auto_https off
}

:{$PORT:8080} {
	# Route PHP files directly
	@php {
		path *.php
	}

	php_fastcgi @php {
		root /app
	}

	# Serve static files
	file_server {
		root /app
	}

	# Try files pattern
	try_files {path} {path}/ /index.php
}
